{
  "testCases": [
    {
      "id": "TC-0001",
      "title": "手机号+正确验证码登录成功（POST /api/login/sms 正向）",
      "preconditions": [
        "测试手机号 13800000001 为已注册可登录账号（测试数据）",
        "测试环境提供有效验证码获取通道（mock/桩）：analyze.json openQuestions 指出验证码发送/获取机制未定义（未验证），本用例前置依赖测试通道返回正确验证码",
        "POST /api/login/sms 接口已部署且可达"
      ],
      "execution_level": "auto",
      "priority": "P0",
      "coverageRef": ["PAY-100"],
      "steps": [
        { "action": "通过测试通道获取手机号 13800000001 的有效验证码", "data": { "phone": "13800000001", "channel": "test-mock" } },
        { "action": "POST /api/login/sms", "data": { "phone": "13800000001", "code": "<valid_code>" } }
      ],
      "expected": [
        "HTTP 200",
        "登录成功：响应包含登录凭据（token/session 等），成功标志存在且非空（响应体结构未验证，最小可执行断言为 HTTP 200 + 登录成功标志存在）"
      ],
      "data": "{\"sourceRef\": \"analyze.json#/boundaries/in/0\", \"acceptance\": \"PAY-100: 手机号+验证码可登录成功\", \"requirementId\": \"PAY-100\", \"note\": \"coverageRef 使用 analyze.json 中真实需求点 id PAY-100（jira:PAY-100，见 analyze.json#/scope），不再使用不存在的 REQ-PAY-100-* 派生 id；验证码获取通道未定义（未验证），前置依赖测试 mock\"}",
      "cleanup": "登出/销毁本次会话产生的 token（如适用）"
    },
    {
      "id": "TC-0002",
      "title": "手机号+错误验证码登录返回 400（POST /api/login/sms 异常）",
      "preconditions": [
        "测试手机号 13800000001 为已注册可登录账号（测试数据）",
        "POST /api/login/sms 接口已部署且可达"
      ],
      "execution_level": "auto",
      "priority": "P0",
      "coverageRef": ["PAY-100"],
      "steps": [
        { "action": "POST /api/login/sms（验证码为确定错误值）", "data": { "phone": "13800000001", "code": "000000" } }
      ],
      "expected": [
        "HTTP 400",
        "登录失败：不返回登录凭据（响应体错误结构未定义，未验证；最小可执行断言为 HTTP 400 + 无登录成功凭据）"
      ],
      "data": "{\"sourceRef\": \"analyze.json#/boundaries/in/1\", \"acceptance\": \"PAY-100: 验证码错误返回 400\", \"requirementId\": \"PAY-100\", \"note\": \"coverageRef 使用 analyze.json 中真实需求点 id PAY-100；若接口对验证码格式有校验导致 000000 被格式层拦截，改用格式正确但内容错误的验证码；响应体错误结构未定义（未验证），断言以 HTTP 400 + 无凭据为最小可执行标准\"}",
      "cleanup": "无（未产生会话）"
    },
    {
      "id": "TC-0003",
      "title": "既有密码登录回归不受影响（密码登录）",
      "preconditions": [
        "测试账号 13800000001 存在且密码已知（测试数据）",
        "原有密码登录接口（如 POST /api/login，密码方式）已部署",
        "密码登录改造前基线行为已知（登录成功）"
      ],
      "execution_level": "auto",
      "priority": "P0",
      "coverageRef": ["PAY-100"],
      "steps": [
        { "action": "使用手机号+密码调用原有密码登录接口", "data": { "username": "13800000001", "password": "<test_password>" } }
      ],
      "expected": [
        "登录成功：HTTP 200 且返回登录凭据（与改造前行为一致）",
        "密码登录未被手机号+验证码改造影响（changePoints: 原有密码登录保留）"
      ],
      "data": "{\"sourceRef\": \"analyze.json#/boundaries/in/2\", \"acceptance\": \"PAY-100: 密码登录不受影响\", \"requirementId\": \"PAY-100\", \"note\": \"coverageRef 使用 analyze.json 中真实需求点 id PAY-100；回归用例，基线为改造前密码登录成功行为；changePoints 见 analyze.json#/boundaries/in/2\"}",
      "cleanup": "登出/销毁本次会话产生的 token（如适用）"
    }
  ],
  "reusedCases": [],
  "coverageMatrix": {
    "PAY-100": ["TC-0001", "TC-0002", "TC-0003"]
  },
  "gaps": [],
  "inputs": {
    "analyze": "unverified:design-env-lacks-hash-tool;orchestrator-must-recompute-analyze-digest-per-G-08"
  }
}
